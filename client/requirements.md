## Packages
date-fns | Date manipulation and formatting
react-day-picker | Date picker component
lucide-react | Icons
clsx | Class name utility
tailwind-merge | Class name utility

## Notes
- Frontend needs to handle complex form calculations (qty * rate = amount) in real-time.
- The default list of "Fixed Items" needs to be pre-populated when creating a NEW report, but editable.
- Routes are defined in shared/routes.ts, use `api.reports.*` for everything.
